node_modules/
dist/
out/
.DS_Store
*.log
# Local secrets — your API keys live here, never commit them
.env
config/settings.local.json
# Runtime data (may contain user-entered keys / sessions) — keep local only
jarvis-data.json
wwebjs_auth/
.wwebjs_cache/
